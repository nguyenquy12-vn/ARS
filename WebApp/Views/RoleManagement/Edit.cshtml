@model Services.DTOs.Role.RoleDetailsResponse

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Edit Role";
}

<div class="card">
    <div class="card-body">
        <form method="post">
            <div class="mb-3">
                <label class="form-label">Name</label>
                <input asp-for="Role.Name" class="form-control" />
            </div>
            <div class="mb-3">
                <label class="form-label">Displayed Name</label>
                <input asp-for="Role.DisplayedName" class="form-control" />
            </div>
            <div class="mb-3">
                <label class="form-label">Description</label>
                <textarea asp-for="Role.Description" class="form-control"></textarea>
            </div>

            <h5>Permissions</h5>
            <div class="row">
                @foreach (var p in Model.Permissions)
                {
                    <div class="col-md-4 mb-2">
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" name="permissions" value="@p.Id" id="p_@p.Id" @(p.Assigned ? "checked" : "") />
                            <label class="form-check-label" for="p_@p.Id">@p.Name</label>
                            <div class="text-muted small">@p.Description</div>
                        </div>
                    </div>
                }
            </div>

            <div class="mt-3">
                <button class="btn btn-primary">Save</button>
                <a class="btn btn-secondary ms-2" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>
